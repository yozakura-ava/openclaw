# Fork delta manifest — 2026.9.5 migration

Captured 2026-09-19 UTC from merge base `7af9cef3040e90902e83f86c8ad48fccacc7acb2` through the archived fork main `037d7bef732f50c4da7f95b344dc0b52d3b18347`. The range contains 110 commits, including release preparation and merge wrappers. The migration base is the signed upstream tag `v2026.9.5` at `ec9c1a13db8938e5a3eaa51fca2e981cde2395a9`.

This is a re-application record, not a raw log. Merge wrappers, release metadata, translations, and CI workarounds were reviewed as fork-local history; only the selected behavior below is carried to the migration branch.

## Disposition

| Area                                                                                                | Disposition              | Migration action                                                              |
| --------------------------------------------------------------------------------------------------- | ------------------------ | ----------------------------------------------------------------------------- |
| Upstream 9.3–9.5 release preparation, dependency refreshes, UI refactors, and translation refreshes | superseded               | supplied by `v2026.9.5`                                                       |
| Workboard tool registration and fresh-session exposure                                              | portable                 | cherry-picked and regenerated (`5f71fe35859`)                                 |
| Workboard prefix resolution                                                                         | portable                 | cherry-picked (`e746a8c15c2`)                                                 |
| Same-owner expired-claim slot recovery                                                              | portable/manual rework   | ported to the 9.5 SQLite kernel and added regression coverage                 |
| Force-close contract and operator escape hatch                                                      | manual rework            | implemented against the 9.5 store/tool APIs; manifest uses `optional:false`   |
| Scheduled orchestration routing and idempotent card creation                                        | superseded               | 9.5's triage/orchestration path already owns this behavior                    |
| Legacy pipeline dedup strike/cooldown commits                                                       | superseded/manual review | not replayed against the replaced 9.5 dispatcher API                          |
| Fork CI concurrency, ARM admission, and workflow guards                                             | portable                 | cherry-picked (`e6f13a02369`)                                                 |
| Manual deploy bundle                                                                                | portable/manual rework   | restored from the prior implementation and made SHA-only with manifest output |
| Fork release cadence, local deployment policy, and hosted-runner-specific workarounds               | intentionally dropped    | not part of the upstream release base                                         |

## Selected source records

- `5f71fe35859` — register Workboard tools as non-optional for fresh sessions.
- `e746a8c15c2` — resolve 8-character card prefixes on the tool surface.
- `21a6e29b5ab` — source behavior reviewed; its old SQLite layout was not replayed verbatim. The equivalent slot check is implemented in `src/sqlite-store-kernel.ts`.
- `ff8d92df946` — manifest-only force-close registration was insufficient on 9.5; replaced with an API-level implementation and generated contract update.
- `e6f13a02369` — stable PR/fork-main concurrency, fork ARM skip, and focused guard coverage.
- `416e7d1e188` through `da8ec9a10d6` — bundle workflow lineage; the migration version additionally requires a full source SHA and emits a bundle manifest.

## Verification rules

The migration branch must pass workflow validation, focused CI guards, Workboard unit/manifest checks, generated-asset checks, and fresh-session tool exposure checks. No raw CI logs belong in this document or in the repository.
